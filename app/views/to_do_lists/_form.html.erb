<%= form_with(model: to_do_list, local: true, class:'form-inline') do |form| %>

   <% if to_do_list.errors.any? %>
      <div id="error_explanation">
         <h2><%= pluralize(to_do_list.errors.count, "error") %> prohibited this to_do_list from being saved:</h2>

         <ul>
            <% to_do_list.errors.full_messages.each do |message| %>
               <li><%= message %></li>
            <% end %>
         </ul>
      </div>
   <% end %>



   <%= form.hidden_field('user_id', value: current_user.id ) %>

   <div class="field pr-2">
      <%= form.text_field :item, class:'form-control' %>
   </div>

   <div class="">
      <%= form.submit 'Add item', class:'btn btn-primary' %>
   </div>
<% end %>
